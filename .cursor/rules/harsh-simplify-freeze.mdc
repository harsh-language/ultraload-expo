---
description: Intentional code patterns frozen by harsh-simplify-code — do not re-flag or change without user request
globs: src/**/*
alwaysApply: false
---

# harsh-simplify freeze

Durable decisions from `/harsh-simplify-code` **keep as-is** answers. Survives branch deletion and merge.

When the skill loads, matching findings are **frozen** — no re-ask, no auto-fix.

## When to add a row

After a **keep as-is** (or equivalent) decision during `/harsh-simplify-code`, append a row below in the same change.

## When to remove a row

Delete when the pattern no longer exists, you explicitly chose to refactor it, or the file was removed.

## Frozen patterns

| ID | File | Pattern | Reason |
|----|------|---------|--------|
| T3 | `src/navigation/RootNavigator.tsx` | `AppPhase` rendered via `if (splash)` / `if (onboarding)` / implicit `main` return — no exhaustive switch | Launch states stable (splash → onboarding → main); unlikely to add phases |
| Q3 | `src/components/useHomepageOptionsMenu.tsx` | Hook returns `<OptionsMenuDropdown />` as `menu` JSX for caller to render | Single consumer (Work Out); colocate until U3/U4 navigation work |
| Q6 | `src/components/useHomepageOptionsMenu.tsx` | `handleSelect` async reset without try/catch — errors swallowed | Dev-only reset; removed before App Store (U7) |
| Q9 | `src/components/OptionsMenuDropdown.tsx` | Reset menu item ships without `__DEV__` guard | Dev builds only until U7; explicit removal in plan |
| T2 | `src/screens/WorkOutScreen.tsx` / `src/components/OptionsMenuDropdown.tsx` | Title bar and menu keep separate `zIndex` literals (menu above title) | Shared stacking constant/comment rejected; values intentional |
