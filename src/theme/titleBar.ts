import { colors } from './tokens';

/** Figma session-title-bar fill — vertical content-trans-light → bg-trans-1 over bg-1 */
export const titleBarGradientColors = [
  colors['content-trans-light'],
  colors['bg-trans-1'],
] as const;
