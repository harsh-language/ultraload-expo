import { colors } from './tokens';

/** Figma color style `bg-gradient-bottom` — vertical bg-trans-1 → content-trans-light */
export const sheetGradientColors = [
  colors['bg-trans-1'],
  colors['content-trans-light'],
] as const;
