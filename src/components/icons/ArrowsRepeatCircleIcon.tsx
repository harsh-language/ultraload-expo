/** Dev reset action — Central `IconArrowsRepeatCircle` (filled). */
import { IconArrowsRepeatCircle } from 'central-icons-filled/IconArrowsRepeatCircle';
import { createIcon } from './createIcon';

export const ArrowsRepeatCircleIcon = createIcon(IconArrowsRepeatCircle);
