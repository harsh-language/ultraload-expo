/** Figma: arrow-path-up (filled) → Central `IconArrowPathUp` */
import { IconArrowPathUp } from 'central-icons-filled/IconArrowPathUp';
import { createIcon } from './createIcon';

export const ArrowPathUpIcon = createIcon(IconArrowPathUp);
