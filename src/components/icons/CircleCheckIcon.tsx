/** Figma: circle-check (filled) → Central `IconCircleCheck` */
import { IconCircleCheck } from 'central-icons-filled/IconCircleCheck';
import { createIcon } from './createIcon';

export const CircleCheckIcon = createIcon(IconCircleCheck);
