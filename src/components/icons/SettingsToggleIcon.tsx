/** Dev demo-data toggle — Central `IconSettingsToggle1` (filled). */
import { IconSettingsToggle1 } from 'central-icons-filled/IconSettingsToggle1';
import { createIcon } from './createIcon';

export const SettingsToggleIcon = createIcon(IconSettingsToggle1);
