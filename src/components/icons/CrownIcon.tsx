/** Figma: crown, vip (filled) → Central `IconCrown` */
import { IconCrown } from 'central-icons-filled/IconCrown';
import { createIcon } from './createIcon';

export const CrownIcon = createIcon(IconCrown);
