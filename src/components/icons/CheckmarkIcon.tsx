/** Figma: checkmark-2 → Central `IconCheckmark2` (square-outlined, stroke 2) */
import { IconCheckmark2 } from 'central-icons/IconCheckmark2';
import { createIcon } from './createIcon';

export const CheckmarkIcon = createIcon(IconCheckmark2);
