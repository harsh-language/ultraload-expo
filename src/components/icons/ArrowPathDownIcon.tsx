/** Figma: arrow-path-down (filled) → Central `IconArrowPathDown` */
import { IconArrowPathDown } from 'central-icons-filled/IconArrowPathDown';
import { createIcon } from './createIcon';

export const ArrowPathDownIcon = createIcon(IconArrowPathDown);
