import { colors, spacing } from '../../theme/tokens';

/** App-wide icon size — Figma layout/size/icon */
export const ICON_SIZE = spacing.icon;

export const ICON_DEFAULT_COLOR = colors['content-1'];
