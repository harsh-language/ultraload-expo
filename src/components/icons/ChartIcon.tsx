/** Paper History view toggle — Central `IconTrending1` (square-outlined, stroke 2) */
import { IconTrending1 } from 'central-icons/IconTrending1';
import { createIcon } from './createIcon';

export const ChartIcon = createIcon(IconTrending1);
