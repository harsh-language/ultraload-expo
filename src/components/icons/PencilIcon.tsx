/** Figma: pencil → Central `IconPencil` (square-outlined, stroke 2) */
import { IconPencil } from 'central-icons/IconPencil';
import { createIcon } from './createIcon';

export const PencilIcon = createIcon(IconPencil);
